import { on, off, trigger } from './core.js';
class Binder {
    constructor(obj) {
    }

    on() {
    }
    off() {
    }
    trigger() {
    }
    static on;
    static off;
    static trigger;
}

export default Binder;